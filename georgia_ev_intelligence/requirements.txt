# ── Core ───────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1
tqdm>=4.66.0

# ── Phase 1: Extraction ─────────────────────────────────────
pymupdf>=1.26.0           # PDF extraction
python-docx>=1.1.0        # DOCX extraction
pandas>=2.0.0             # CSV + Excel reading
openpyxl>=3.1.0           # Excel reading + eval reports
tavily-python>=0.7.0      # Search + Extract (replaces DDG + trafilatura)
httpx>=0.28.0             # Async HTTP client (generator, evaluator, Ollama calls)

# ── Phase 1: Database ───────────────────────────────────────
sqlalchemy>=2.0.0         # ORM
psycopg2-binary>=2.9.0    # PostgreSQL driver (works with Neon + Supabase)

# ── Phase 1: Cloud Storage ──────────────────────────────────
boto3>=1.34.0             # Backblaze B2 (S3-compatible)

# ── Phase 2: Embedding + Vector DB ─────────────────────────
qdrant-client>=1.9.0      # Qdrant vector database client
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0  # Hierarchical chunking

# ── Phase 3: Knowledge Graph ─────────────────────────────────
neo4j>=5.20.0             # Neo4j Python driver
neo4j-graphrag>=1.0.0     # VectorCypherRetriever (official)

# ── Phase 4: LLM Agent ──────────────────────────────────────
langgraph>=0.2.0          # Stateful agent orchestration
langchain-ollama>=0.1.0   # Ollama integration

# ── Phase 5: Chat UI ─────────────────────────────────────────
fastapi>=0.115.0          # API backend
uvicorn[standard]>=0.30.0 # ASGI server
websockets>=12.0          # Streaming support
jinja2>=3.1.0             # HTML templating

# ── Evaluation (custom LLM-as-judge — NO ragas library) ──────
# Pattern adopted from ev_data_LLM_comparsions/src/evaluator.py
# Judge: qwen2.5:14b via Ollama local
# Metrics: faithfulness, answer_relevancy, context_precision, context_recall, answer_correctness

# ── Phase 5: Few-Shot RAG ─────────────────────────────────────
# qdrant-client already listed above (Phase 2)
# Embedding model: nomic-embed-text (run via Ollama, not pip)
#   ollama pull nomic-embed-text
# Optional upgrade: qwen2.5:14b for higher synthesis accuracy
#   ollama pull qwen2.5:14b
