pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
rapidfuzz>=3.0
fastapi>=0.110
uvicorn[standard]>=0.30
requests>=2.28
anthropic>=0.40
sentence-transformers>=3.0
numpy>=1.26
rank-bm25>=0.2.2
einops>=0.8
psycopg2-binary>=2.9
pydantic>=2.0

# RAGAS evaluation — official explodinggradients release (NOT the vibrantlabsai fork).
# 0.1.x uses text-based prompting for all metrics, including context_precision,
# which works correctly with Ollama. The vibrantlabsai 0.4.x fork uses the
# `instructor` library (structured JSON via OpenAI function-calling) for
# context_precision, which is incompatible with Ollama and scores 0 for every chunk.
ragas>=0.1.9,<0.2.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0

# Development / testing
pytest>=8.0
